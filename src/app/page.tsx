import { Hero } from "@/components/hero";

export default function Home() {
  return (
    <main className="relative z-1 mx-auto w-full max-w-[var(--container-narrow)] px-s-6 pb-40">
      <Hero />
    </main>
  );
}
